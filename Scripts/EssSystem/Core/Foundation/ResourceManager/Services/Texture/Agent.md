# TextureService
**Service** (`Service<TextureService>`)
## 职责
- 异步加载 Texture 资源
- 缓存 Texture 对象
## Event API

> Full Event definitions (params / return / side effects / usage) live in root Events.md -> section: **Texture Event**.

- `EVT_GET_TEXTURE_ASYNC`
- `EVT_LOAD_TEXTURE_ASYNC`

## 文件结构
```
Services/Texture/
├── TextureService.cs
├── Agent.md
```
